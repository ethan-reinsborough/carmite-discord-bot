import { Storyboard as IStoryboard } from '../util/types';
/**
 * Organized data of a storyboard.
 */
export declare class Storyboard {
    /**
     * The storyboard's ID.
     */
    id: number;
    /**
     * The total amount of storyboards for the video.
     */
    totalStoryboards: number;
    /**
     * The index of this storyboard in total storyboards.
     */
    sequence: number;
    /**
     * The storyboard image's URL.
     */
    url: string;
    /**
     * An object holding data about the image's frames.
     */
    frames: Record<'width' | 'height' | 'total' | 'horizontal' | 'vertical', number>;
    /** @ignore */
    constructor(raw: IStoryboard);
}
