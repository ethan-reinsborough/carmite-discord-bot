import { Server as IServer } from '../util/types';
/**
 * Organized data of a streaming server.
 */
export declare class Server {
    /**
     * The server's raw data.
     */
    readonly raw: IServer;
    /**
     * The server's name.
     */
    name: string;
    /**
     * The server's ID.
     */
    id: number;
    /**
     * The server's slug.
     */
    slug: string;
    /**
     * The server's ratings in EU, Asia, and NA.
     */
    ratings: Record<'eu' | 'asia' | 'na', number>;
    /**
     * Whether the server is permanent.
     */
    permanent: boolean;
    /**
     * An array of streams of the server.
     */
    streams: IServer['streams'];
    /**
     * The server's sequence.
     */
    sequence: number;
    /** @ignore */
    constructor(raw: IServer);
}
