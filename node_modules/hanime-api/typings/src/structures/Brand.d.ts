import { Client } from '../Client';
import { Brand as IBrand, SearchOptions } from '../util/types';
/**
 * Organized data of a brand.
 */
export declare class Brand {
    /**
     * The client that instantiated this brand.
     */
    readonly client: Client;
    /**
     * The brand's ID.
     */
    id: number;
    /**
     * The brand's name.
     */
    title: string;
    /**
     * The brand's slug, used in the URL.
     */
    slug: string;
    /**
     * The brand's website URL.
     */
    website: string | null;
    /**
     * The brand's logo's URL.
     */
    logo: string | null;
    /**
     * The brand's email address.
     */
    email: string | null;
    /**
     * The brand's uploaded videos.
     */
    uploads: number;
    /** @ignore */
    constructor(client: Client, data: IBrand);
    /**
     * Fetch the uploaded videos of the brand.
     * @param str The optional string to search for.
     * @param options The optional options to search with.
     */
    fetchUploads(str?: string, options?: SearchOptions): Promise<import("./PartialVideo").PartialVideo[]>;
}
