import { Client } from '../Client';
import { SearchOptions, Tag as ITag, TagName } from '../util/types';
/**
 * Organized data of a tag.
 */
export declare class Tag {
    /**
     * The client that instantiated this tag.
     */
    client: Client;
    /**
     * The tag's ID.
     */
    id: number;
    /**
     * The tag's name.
     */
    name: TagName;
    /**
     * The amount of videos tagged with this tag.
     */
    videos: number;
    /**
     * The description of this tag.
     */
    description: string;
    /**
     * The wide image URL of the tag.
     */
    wideImage: string;
    /**
     * The tall image URL of the tag.
     */
    tallImage: string;
    /** @ignore */
    constructor(client: Client, raw: ITag);
    /**
     * Fetch all videos tagged with this tag, optionally extendable with search options.
     * @param str The optional string to search for.
     * @param options The optional options to search with.
     */
    fetchVideos(str?: string, options?: SearchOptions): Promise<import("./PartialVideo").PartialVideo[]>;
}
