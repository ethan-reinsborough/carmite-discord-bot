"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Brand = void 0;
const util_1 = require("../util");
/**
 * Organized data of a brand.
 */
class Brand {
    /** @ignore */
    constructor(client, data) {
        Object.defineProperty(this, 'client', { value: client });
        this.id = data.id;
        this.title = data.title;
        this.slug = data.slug;
        this.website = data.website_url;
        this.logo = data.logo_url;
        this.email = data.email;
        this.uploads = data.count;
    }
    /**
     * Fetch the uploaded videos of the brand.
     * @param str The optional string to search for.
     * @param options The optional options to search with.
     */
    fetchUploads(str = '', options = {}) {
        return this.client.searchAll(str, util_1.resolveSearchOptions(str, options, { brands: [this.title] }));
    }
}
exports.Brand = Brand;
